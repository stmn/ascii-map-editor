# v2.6 IO Scopes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Decyzja usera z 2026-08-06: zakresy eksportu/importu to LEVEL i PROJECT; workspace znika z UI i z API. Import project przyjmuje takze stare pliki workspace (kompatybilnosc wsteczna dla backupow z 2.2-2.5).

**Architecture:** `exportProject`/`importProject` w core/store.ts jako refaktor istniejacej maszynerii workspace (wspolna logika zostaje, warianty to wrappery); exportWorkspace/importWorkspace usuniete wraz z przerobionymi testami. UI: rename wiersza level, nowy wiersz project, wiersz workspace usuniety.

**Tech Stack:** jak dotad. Zero runtime deps.

## Global Constraints

- WSZYSTKO LOKALNIE. Commity lokalne, angielskie, jednolinijkowe, bez Co-Authored-By.
- UI po angielsku; komentarze PL bez diakrytykow; NIGDY dlugich myslnikow. Stylistyka v1.
- Headless Chrome zawsze z --use-mock-keychain; jedna sesja CDP per task UI; kill + ps.
- Zero duplikacji: project-scope na wspolnej logice serializacji/walidacji/merge-add.
- Branch: `v26-io-scopes` od `main`. Po ukonczeniu: lokalny merge.

## File Structure

```
src/core/store.ts            # MOD (T1): exportProject/importProject; workspace API usuniete
tests/store.test.ts          # MOD (T1): testy project-scope + legacy-workspace acceptance
src/ui/panels/project.ts     # MOD (T2): wiersze Export/Import level... i Export/Import project
src/ui/panels/exportModal.ts # MOD (T2): plik poziomu level.json
README.md, itch-page.md, package.json  # MOD (T3)
```

---

### Task 0: Branch

- [ ] **Step 1:** `cd /Users/darek/Code/level-editor && git checkout -b v26-io-scopes`

---

### Task 1: Core project-scope IO (TDD) + usuniecie workspace API

**Files:**
- Modify: `src/core/store.ts`, `tests/store.test.ts`

**Interfaces:**
- Produces:

```ts
export async function exportProject(store: WorkspaceStore, projectId: string): Promise<string>
// { app: 'ascii-level-editor-project', version: 1, project: { name }, levels: [LevelRecord bez thumb] }
// Rzuca Error('Project not found') gdy brak projektu.

export async function importProject(store: WorkspaceStore, json: string, now: number): Promise<{ projects: number; levels: number }>
// Przyjmuje: (a) plik project v1 (jak wyzej) -> dodaje JEDEN projekt (nowe id, nextName przy kolizji,
// poziomy z nowymi id/remapem, walidacja rekordow jak dotad); (b) LEGACY plik workspace
// ('ascii-level-editor-workspace') -> dodaje WSZYSTKIE jego projekty ta sama sciezka (merge-add).
// Rzuca Error('Unrecognized project file') dla obcych formatow.
```

- exportWorkspace/importWorkspace: USUNIETE z API. Wspolna logika (walidacja rekordow, remap id, nextName, thumb: null) wyciagnieta do wewnetrznych helperow uzywanych przez importProject - zadnych kopii. Testy workspace przerobione na project-scope; przypadek legacy-workspace pokryty jawnym testem (plik w formacie workspace importuje 2 projekty z poziomami).

- [ ] **Step 1: TDD** - testy: roundtrip project (export -> import do drugiego store'a: 1 projekt, poziomy, nextName przy kolizji nazwy, nowe id, thumb null w pliku); Project not found; legacy workspace z 2 projektami -> {projects: 2, levels: N}; obcy format -> throw; walidacja zepsutych rekordow (jak dotychczasowy test importWorkspace, przeniesiony).
- [ ] **Step 2: FAIL -> implementacja -> PASS.** `npm test` + `npm run build`... UWAGA: project.ts (UI) uzywa exportWorkspace/importWorkspace - usuniecie API zepsuje build do Taska 2. Dopuszczalny stan przejsciowy jak w poprzednich etapach: `npx tsc --noEmit 2>&1 | grep -v 'src/ui/'` puste; pelny build wraca w T2.
- [ ] **Step 3: Commit.** `git add -A && git commit -m "Replace workspace io with project scoped export and import"`

---

### Task 2: UI - wiersze level/project, bez workspace

**Files:**
- Modify: `src/ui/panels/project.ts`, `src/ui/panels/exportModal.ts`, `src/styles.css` (jesli trzeba)

**Zachowanie:**
- Karta Project, wiersze IO (w miejscu dotychczasowych dwoch): `[Export level...] [Import level...]` (istniejace modale, tylko etykiety) oraz ponizej `[Export project] [Import project]` (nowe: exportProject biezacego projektu -> download `project.json`; importProject z file inputu .json -> toast `Imported P projects, L levels` / czerwony toast przy bledzie; refresh karty; snapshot licznika bledow zapisu jak przy dawnym imporcie workspace). Wiersz workspace USUNIETY.
- Plik poziomu z modalu Export level: nazwa `level.json` (zamiast project.json). parseProject bez zmian (przyjmuje wszystko).
- Degenerate warianty karty: wiersz level widoczny (dziala na poziomie w pamieci), wiersz project ukryty/disabled gdy brak store'a (adnotacja w kodzie).
- KRYTERIUM: pelny `npm run build` znow zielony (koniec stanu przejsciowego).

- [ ] **Step 1:** Implementacja + style.
- [ ] **Step 2:** Weryfikacja: `npm test` + `npm run build`; JEDNA sesja CDP (--use-mock-keychain): wiersze i etykiety; Export level pobiera level.json; Export project pobiera project.json; Import project wczytuje go (nowy projekt w dropdownie); import STAREGO pliku workspace (wygeneruj w tescie strukture legacy) dodaje wszystkie projekty; zly plik -> czerwony toast; brak wiersza workspace; zero bledow konsoli; kill + ps.
- [ ] **Step 3: Commit.** `git add -A && git commit -m "Rename io rows to level and project scopes"`

---

### Task 3: Docs + wersja 2.6.0 + pakowanie

**Files:**
- Modify: `README.md` (sekcja Projects: nowe zakresy IO, nota kompatybilnosci ze starymi plikami workspace, level.json), `itch-page.md` (changelog "v2.6: level and project export scopes"), `package.json` (`"version": "2.6.0"`)

- [ ] **Step 1:** Docs (ASCII, fact-check) + wersja.
- [ ] **Step 2:** `npm run zip`; `unzip -l`; standalone file:// check (jedna sesja, --use-mock-keychain, kill + ps).
- [ ] **Step 3:** `npm test` + `npm run build` zielone.
- [ ] **Step 4: Commit.** `git add -A && git commit -m "Document io scopes and bump version to 2.6.0"`

---

## Self-Review (wykonany)

- Decyzja usera pokryta: level + project, workspace out; kompatybilnosc wsteczna backupow przez legacy-acceptance w importProject.
- Placeholdery: brak. Stan przejsciowy T1->T2 jawny (wzorzec z etapu A).
- Zero duplikacji: wspolne helpery importu; project.json (poziom) -> level.json bez zmian parsera.
