# v2.7.1 Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cztery szlify z backlogu review v2.6/v2.7 (decyzja usera 2026-08-06: "zrob te szlify tylko"): kolejnosc projektow przy imporcie legacy backupu, klik w mape przy otwartym menu nie maluje, panel menu zamyka sie przy scroll/resize, wygladzenie noty w README.

**Architecture:** Zmiany punktowe: store.ts (1 linia + test), menu.ts (2 zachowania), README. Wersja 2.7.1.

**Tech Stack:** jak dotad. Zero runtime deps.

## Global Constraints

- WSZYSTKO LOKALNIE: zadnego push/remote. Commity lokalne, angielskie, jednolinijkowe, bez Co-Authored-By.
- Komentarze PL bez diakrytykow; NIGDY dlugich myslnikow - tylko "-". Docs ASCII.
- Headless Chrome ZAWSZE z --use-mock-keychain; wlasny vite --port 5190 --strictPort (serwer usera na 5173 NIETYKALNY); jedna sesja CDP; po niej kill + ps/lsof.
- Branch: `v271-polish` od `main`. Po ukonczeniu: lokalny merge.

---

### Task 1: Cztery szlify + wersja + pakowanie

**Files:**
- Modify: `src/core/store.ts`, `tests/store.test.ts`, `src/ui/menu.ts`, `README.md`, `package.json`

**Zachowanie:**
1. **Kolejnosc projektow z legacy backupu (TDD):** importProject dla pliku legacy-workspace nadaje kazdemu kolejnemu projektowi createdAt = now + index (zamiast identycznego now dla wszystkich) - dropdown sortuje po createdAt, wiec kolejnosc projektow z pliku przezywa import. Test: legacy plik z 2 projektami w znanej kolejnosci -> po imporcie listProjects posortowane po createdAt daje te sama kolejnosc; timestamps roznia sie o 1.
2. **Klik w mape przy otwartym menu tylko zamyka:** w menu.ts sciezka zamykania na pointerdown poza panelem/triggerem - gdy target jest canvasem mapy (element #map), zdarzenie jest POLYKANE (stopPropagation w capture; listener menu jest na document capture, wiec wyprzedza handler canvasa) - menu sie zamyka, gest malowania NIE startuje. Klik w inne elementy UI (przyciski, selecty) dziala jak dotad (zamyka menu i wykonuje akcje).
3. **Panel nie sledzi triggera - zamykanie na scroll/resize:** przy otwartym panelu dowolny scroll (listener scroll na document w capture, sidebar scrolluje wewnetrznie) oraz window resize zamykaja menu (closeSelf). Listenery dodawane przy otwarciu, zdejmowane przy zamknieciu (jak pozostale).
4. **README:** nota przy Floating center button - usunac z listy zrodel rozcentrowania "switching mode" i "dragging a card between columns" (shiftView zachowuje centrowanie); zostawic pan/zoom/resize okna.
5. `package.json` version "2.7.1"; `npm run zip` + `unzip -l`.

- [ ] **Step 1:** TDD pkt 1 (test FAIL -> fix -> PASS).
- [ ] **Step 2:** menu.ts pkt 2-3; README pkt 4; wersja pkt 5.
- [ ] **Step 3:** Weryfikacja: `npm test` + `npm run build`; JEDNA sesja CDP: otwarte menu + pointerdown na canvasie -> menu zamkniete, zadna komorka nie namalowana (porownanie canvas przed/po); otwarte menu + scroll sidebara/resize -> zamkniete; normalny klik w przycisk przy otwartym menu dziala; zero bledow konsoli; kill + ps/lsof.
- [ ] **Step 4:** `npm run zip`. Commit: `git add -A && git commit -m "Polish legacy import order and menu close behavior"`.

---

## Self-Review (wykonany)

- 4 szlify pokryte; brak placeholderow; pkt 2 opiera sie na kolejnosci capture document > target handler (zweryfikowane w istniejacym kodzie menu.ts, ktory juz uzywa capture).
