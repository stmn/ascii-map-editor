# v2.7 UI Tweaks Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Batch prosb usera z 2026-08-06: (1) Rooms dotyczy tylko Dungeona - przegrupowac karte Generate; (2) nowy domyslny uklad sidebarow: lewo Project/Generate/Draw, prawo Legend/Layers/Level(io); (3) etykiety Export/Import level bez "..." i osobna karta Level po prawej; (4) tekst w przyciskach sidebara nigdy w 2 liniach; (5) Draw bez listy znakow (chips recent); (6) narzedzie gumki - po zaznaczeniu wymazuje bez skrotu klawiszowego.

**Architecture:** Zmiany punktowe na istniejacych modulach + nowa mala karta Level (io poziomu przeniesione z karty Project). Domyslny uklad przez przestawienie kart w index.html (lewa kolumna przestaje startowac pusta) + bump klucza localStorage layoutu (jednorazowy reset ukladow userow - swiadoma decyzja produktowa). Gumka jako stan narzedzia w editorState + odczyt w InputController.

**Tech Stack:** jak dotad. Zero runtime deps.

## Global Constraints

- WSZYSTKO LOKALNIE: zadnego push/remote. Commity lokalne, angielskie, jednolinijkowe, bez Co-Authored-By.
- UI po angielsku; komentarze PL bez diakrytykow; NIGDY dlugich myslnikow - tylko "-". Stylistyka v1.
- Headless Chrome ZAWSZE z --use-mock-keychain; jedna krotka sesja CDP per task UI; po sesji kill serwera+Chrome i dowod ps/lsof.
- Nowe mutacje siatki przez historie jak dotad; zero duplikacji logiki.
- Ikony: inline Lucide w src/ui/icons.ts, STROKE_WIDTH='3' (konwencja pliku).
- Branch: `v27-ui-tweaks` od `main`. Po ukonczeniu: lokalny merge.

## File Structure

```
src/ui/panels/generate.ts    # MOD (T1): Rooms przegrupowane pod Dungeon
index.html                   # MOD (T2): karty rozdzielone na dwie kolumny, nowa karta level
src/ui/layout.ts             # MOD (T2): bump STORAGE_KEY + czyszczenie starego klucza
src/ui/panels/level.ts       # NOWY (T2): karta Level z [Export level] [Import level]
src/ui/panels/project.ts     # MOD (T2): modalRow usuniety (io poziomu wyprowadzone)
src/ui/panels.ts             # MOD (T2): wpiecie karty Level
src/styles.css               # MOD (T2): level w liscie ukrywanych w Simplified; przyciski 1-liniowe
src/core/editorState.ts      # MOD (T3): pole tool 'brush' | 'eraser'
src/ui/input.ts              # MOD (T3): gumka z narzedzia, nie tylko z modyfikatora
src/app.ts                   # MOD (T3): przekazanie eraserActive do InputController
src/ui/icons.ts              # MOD (T3): ikona eraser (i pencil juz jest)
src/ui/panels/draw.ts        # MOD (T3): bez chips recent; przelacznik narzedzia
src/ui/mode.ts lub panels.ts # MOD (T3): reset narzedzia na brush przy wejsciu w Simplified
README.md, itch-page.md, package.json  # MOD (T4)
```

---

### Task 0: Branch

- [ ] **Step 1:** `cd /Users/darek/Code/level-editor && git checkout -b v27-ui-tweaks main`

---

### Task 1: Generate - Rooms tylko przy Dungeonie

**Files:**
- Modify: `src/ui/panels/generate.ts`

**Zachowanie:**
- Rooms dotyczy WYLACZNIE Dungeona, a obecny uklad (W, H, Rooms, [Maze][Dungeon]) sugeruje, ze wszystkich generatorow. Nowa kolejnosc w karcie: pola W i H (bez zmian, field-col) -> przycisk `Maze` na pelna szerokosc (wlasny btn-row) -> pole `Rooms` (labeledStack, jak dotad) -> przycisk `Dungeon` na pelna szerokosc (wlasny btn-row) -> hint bez zmian. Rooms sasiaduje wtedy tylko z Dungeonem i grupowanie jest jasne.
- Logika generate(kind) bez zmian (roomTarget dalej tylko dla dungeon).

- [ ] **Step 1:** Przestawic skladanie karty w initGenerate zgodnie z opisem.
- [ ] **Step 2:** Weryfikacja: `npm test` + `npm run build` (bez sesji CDP - czysta zmiana kolejnosci; wyglad potwierdzi sesja Taska 2).
- [ ] **Step 3: Commit.** `git add -A && git commit -m "Group rooms input with dungeon button"`

---

### Task 2: Domyslny uklad kolumn + karta Level + przyciski 1-liniowe

**Files:**
- Modify: `index.html`, `src/ui/layout.ts`, `src/ui/panels/project.ts`, `src/ui/panels.ts`, `src/styles.css`
- Create: `src/ui/panels/level.ts`

**Zachowanie:**
1. **Domyslny uklad:** index.html - `#sidebar-left` dostaje karty (w tej kolejnosci): project, generate, draw. `#sidebar` (prawa): map, extra (te dwie zostaja pierwsze - Simplified pokazuje je na gorze jak w v1), legend, layers, level (nowa). Komentarz o "lewa kolumna startuje pusta" do aktualizacji.
2. **Reset zapisanych ukladow:** layout.ts - STORAGE_KEY zmienia sie na `'ascii-level-editor-layout2'`; przy odczycie (readLayout) usun stary klucz `'ascii-level-editor-layout'` jesli istnieje (jednorazowe czyszczenie; swiadoma decyzja: userzy dostaja nowy domyslny uklad, reczne uklady resetuja sie raz). Mechanika DnD/zapisu bez zmian.
3. **Karta Level:** nowy `src/ui/panels/level.ts` z `initLevelIo(box: HTMLElement, modals: { openExport(): void; openImport(): void }): void` - jeden btn-row: `button('Export level', '', modals.openExport)` + `button('Import level', 'success', modals.openImport)` (BEZ trzech kropek w etykietach) + hint `'Current level only - use the Project card for whole projects.'`. Karta w index.html: `<details open data-section="level"><summary>Level</summary><div class="section-body" id="panel-level"></div></details>`. Wpiecie w panels.ts obok istniejacych initow (modale export/import juz zwracaja open()).
4. **project.ts:** modalRow() USUNIETY w calosci (wszystkie 3 uzycia: galaz bez projektow, glowna, galaz bez store'a) - io poziomu mieszka teraz w karcie Level, ktora dziala zawsze (modale operuja na poziomie w pamieci). Rzad [Export project] [Import project] bez zmian. Galezie degenerate zostaja z samymi hintami.
5. **Simplified:** styles.css - do listy ukrywanych sekcji w `body.mode-simplified ...` (linie ~851-855) dodac `details[data-section='level']`.
6. **Przyciski 1-liniowe:** zadna etykieta przycisku w kartach sidebara (`Export level`, `Import level`, `Export project`, `Import project`, `Maze`, `Dungeon`, `Clear layer`, przyciski kart Project/Layers itd.) nie moze lamac sie do 2 linii przy domyslnej szerokosci kart. Srodek: dla `.btn-row button` (i pokrewnych przyciskow kart) zmniejszyc font-size o stopien (np. 11px -> 9px, spojnie z istniejaca skala 9/11/13) i/lub `white-space: nowrap` - dobrac tak, zeby NIC nie wystawalo poza przycisk (bez obcinania tekstu). Weryfikacja wizualna obowiazkowa.
7. Centrowanie: przestawienie kart do lewej kolumny zmienia szerokosci - istniejaca sciezka centerOnPaper przy bootcie liczy z aktualnych szerokosci (nic do roboty poza weryfikacja, ze mapa startuje wysrodkowana miedzy kolumnami).

- [ ] **Step 1:** index.html + layout.ts (klucz + czyszczenie).
- [ ] **Step 2:** level.ts + panels.ts + project.ts + styles.css.
- [ ] **Step 3:** Weryfikacja: `npm test` + `npm run build`; JEDNA sesja CDP (--use-mock-keychain, swiezy profil): (a) domyslny uklad - lewo: Project, Generate, Draw; prawo: Legend, Layers, Level (screenshot); (b) mapa wysrodkowana miedzy kolumnami; (c) Export level pobiera level.json, Import level otwiera modal (bez kropek w etykietach); (d) zadna etykieta przycisku w sidebarach nie ma 2 linii (screenshot + sprawdz offsetHeight kilku przyciskow == jednoliniowy); (e) localStorage ze STARYM kluczem layoutu -> po bootcie klucz usuniety, nowy uklad obowiazuje; (f) Simplified nie pokazuje karty Level; (g) zero bledow konsoli; kill + ps/lsof.
- [ ] **Step 4: Commit.** `git add -A && git commit -m "Split default sidebar layout and move level io to own card"`

---

### Task 3: Draw - bez listy znakow, z gumka

**Files:**
- Modify: `src/ui/panels/draw.ts`, `src/core/editorState.ts`, `src/ui/input.ts`, `src/app.ts`, `src/ui/icons.ts`, `src/ui/mode.ts` (lub panels.ts - tam gdzie mieszka przelaczenie trybu)

**Zachowanie:**
1. **Lista znakow OUT:** z draw.ts znikaja: pasek chips recent, DEFAULT_RECENT, MAX_RECENT, pushRecent, renderChips. renderChipRow ZOSTAJE (uzywa go pasek rozmiarow). DrawPanel.render() renderuje odtad rozmiary i przelacznik narzedzia. Uklad karty: [przelacznik narzedzia] -> Char -> Size -> Clear layer -> hint.
2. **Stan narzedzia:** editorState - nowe pole `tool: 'brush' | 'eraser'` (default 'brush', sesyjne jak brush/brushSize).
3. **Przelacznik:** w draw.ts rzad dwoch przyciskow ikonowych (icons.ts: `pencil` juz jest; DODAC `eraser` - sciezki SVG z Lucide "eraser", builderem createElementNS jak reszta, STROKE_WIDTH 3): Brush / Eraser, aria-label i title ('Brush tool' / 'Eraser tool'), aktywne narzedzie wyroznione (klasa active jak chips). Klik ustawia state.tool i przerysowuje.
4. **Gumka dziala bez skrotu:** input.ts - InputCallbacks dostaje `eraserActive(): boolean`; w down() gest jest gumka gdy `isEraseModifier(e) || cb.eraserActive()`; w move() flaga hover erasing analogicznie uwzglednia cb.eraserActive(). app.ts przekazuje `eraserActive: () => state.tool === 'eraser'`. Skrot Alt/Ctrl dziala DALEJ niezaleznie od narzedzia. Sciezka erase/paint, historia strokeCommand, hover-podswietlenie gumki - bez zmian (juz istnieja).
5. **Powrot do pedzla:** setBrush (kazda sciezka: klawisz znaku, pole Char, klik w legende, pole Character z karty Map) przelacza state.tool z powrotem na 'brush' - wpisanie znaku wyraza zamiar rysowania. Przelaczenie trybu na Simplified TEZ resetuje tool na 'brush' (karta Draw jest tam ukryta - gumka nie moze zostac wlaczona bez widocznej kontrolki); reset w miejscu, gdzie przelaczany jest tryb.
6. **Hint:** tekst na `'Press any character key to switch the brush. Alt or Ctrl + drag also erases.'`.

- [ ] **Step 1:** editorState + input.ts + app.ts (przewod eraserActive).
- [ ] **Step 2:** icons.ts (eraser) + draw.ts (usuniecie chips, przelacznik) + reset przy trybie Simplified.
- [ ] **Step 3:** Weryfikacja: `npm test` + `npm run build`; JEDNA sesja CDP (--use-mock-keychain): (a) karta Draw bez paska znakow, z przelacznikiem (screenshot); (b) Eraser aktywny -> przeciagniecie wymazuje BEZ modyfikatora, undo cofa; (c) wcisniecie klawisza znaku wraca do Brush (przelacznik sie przestawia) i maluje; (d) w trybie Brush Alt+drag dalej wymazuje; (e) wybor gumki -> przelaczenie na Simplified -> rysowanie w replice v1 MALUJE (tool zresetowany); (f) zero bledow konsoli; kill + ps/lsof.
- [ ] **Step 4: Commit.** `git add -A && git commit -m "Replace recent chars with tool switch and add eraser"`

---

### Task 4: Docs + wersja 2.7.0 + pakowanie

**Files:**
- Modify: `README.md` (karta Level, nowy domyslny uklad kolumn, gumka, Generate po przegrupowaniu, Draw bez recent chips; drzewo plikow +level.ts), `itch-page.md` (changelog "v2.7: eraser tool, split sidebar layout, level io card"), `package.json` (`"version": "2.7.0"`)

- [ ] **Step 1:** Docs (ASCII, fact-check przeciw kodowi) + wersja.
- [ ] **Step 2:** `npm run zip`; `unzip -l`; standalone file:// check (jedna sesja, --use-mock-keychain, screenshot, kill + ps).
- [ ] **Step 3:** `npm test` + `npm run build` zielone.
- [ ] **Step 4: Commit.** `git add -A && git commit -m "Document v2.7 ui tweaks and bump version"`

---

## Self-Review (wykonany)

- 6 prosb usera pokrytych: Rooms/Dungeon (T1), domyslny uklad lewo/prawo (T2), Export/Import level bez kropek w osobnej karcie po prawej (T2), przyciski 1-liniowe (T2), Draw bez listy znakow (T3), gumka bez skrotu (T3).
- Placeholdery: brak; sygnatura initLevelIo i pole tool jawne; kolizje nazw brak.
- Ryzyka nazwane: reset zapisanych ukladow (decyzja swiadoma, bump klucza + czyszczenie), gumka w Simplified (reset przy przelaczeniu trybu), map/extra zostaja pierwsze w prawej kolumnie dla wygladu v1.
