# v2.5 Tweaks Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cztery prosby usera z 2026-08-06: mniejszy Character do lewej (Simplified), przyciski Export/Import w karcie Project (likwidacja osobnych kart), liczba pokojow w generatorze Dungeon (silnik + oba UI), plywajacy przycisk Center na dole posrodku.

**Architecture:** Zmiany punktowe na istniejacych modulach; jedyna logika silnikowa to parametr roomTarget w generateDungeon (TDD).

**Tech Stack:** jak dotad. Zero runtime deps.

## Global Constraints

- WSZYSTKO LOKALNIE: zadnego push/remote. Commity lokalne, angielskie, jednolinijkowe, bez Co-Authored-By.
- UI po angielsku; komentarze PL bez diakrytykow; NIGDY dlugich myslnikow. Stylistyka v1; bez gradientow/box-shadow.
- Headless Chrome w weryfikacjach ZAWSZE z --use-mock-keychain; jedna krotka sesja per task UI; kill + ps.
- Nowe mutacje przez historie jak dotad; zero duplikacji logiki.
- Branch: `v25-tweaks` od `main`. Po ukonczeniu: lokalny merge.

## File Structure

```
src/core/generators.ts       # MOD (T1): roomTarget
src/ui/panels/{generate,extra}.ts  # MOD (T1): input Rooms
src/ui/panels/map.ts         # MOD (T2): Character mniejszy/do lewej
src/ui/panels/project.ts     # MOD (T2): przyciski Export.../Import...
src/ui/panels/{exportModal,importModal}.ts  # MOD (T2): init bez wlasnych kart
src/ui/center.ts             # NOWY (T2): plywajacy przycisk Center
index.html, src/styles.css, src/ui/mode.ts, src/ui/layout.ts  # MOD (T2)
tests/generators.test.ts     # MOD (T1)
```

---

### Task 0: Branch

- [ ] **Step 1:** `cd /Users/darek/Code/level-editor && git checkout -b v25-tweaks`

---

### Task 1: Liczba pokojow w Dungeonie (silnik TDD + oba UI)

**Files:**
- Modify: `src/core/generators.ts`, `src/ui/panels/generate.ts`, `src/ui/panels/extra.ts`
- Test: `tests/generators.test.ts`

**Zachowanie:**
- `generateDungeon` zyskuje opcjonalny parametr `roomTarget` (po minRoom/maxRoom; default `null`/undefined = dotychczasowe zachowanie roomTries). Gdy podany (>=1): generator probuje stawiac pokoje az OSIAGNIE roomTarget pokoi albo wyczerpie wewnetrzny limit prob (`roomTarget * 25`); roomTries ignorowane w tym trybie. Zwracana mapa jak dotad.
- TDD: (a) `roomTarget: 3` na duzej planszy (60x40, seed) daje DOKLADNIE 3 pokoje - policz przez zliczenie rozlacznych obszarow podlogi? Prosciej: pokoje moga sie stykac korytarzami - liczenie pokoi z mapy zawodne. Zamiast tego generator ZWRACA liczbe postawionych pokoi: zmien sygnature zwrotu? NIE - istniejace wywolania. Rozwiazanie: nowy eksport `generateDungeonDetailed(w, h, opts): { grid: Grid; roomsPlaced: number }` z opcjami `{ roomTries?, rng?, minRoom?, maxRoom?, roomTarget? }`; stary `generateDungeon(w,h,roomTries,rng,minRoom,maxRoom)` staje sie cienkim wrapperem (zero duplikacji petli). Test: roomsPlaced === 3 przy roomTarget 3 (duza plansza, seed); roomsPlaced <= roomTarget zawsze; ciasna plansza (10x8, roomTarget 50) konczy bez zawieszenia z roomsPlaced < 50; istniejace testy bez zmian.
- UI Advanced (`generate.ts`): input `Rooms:` (numberInput, default 8, min 1 max 50, full width jak W/H) widoczny nad przyciskami; Dungeon uzywa roomTarget z inputu; Maze go ignoruje.
- UI Simplified (`extra.ts`): w sekcji Dungeon generator dodatkowy input `Rooms:` (default 8) nad Min. room size; przekazywany jako roomTarget.
- Wspolna sciezka runGenerator bez zmian (parametr przechodzi przez istniejace wywolania).

- [ ] **Step 1:** TDD (test -> FAIL -> implementacja -> PASS; istniejace testy zielone bez modyfikacji).
- [ ] **Step 2:** Oba UI + przekazanie parametru.
- [ ] **Step 3:** Weryfikacja: `npm test` + `npm run build`; bez sesji CDP (logika pokryta testami, inputy sprawdzi T2/finalna sesja).
- [ ] **Step 4: Commit.** `git add -A && git commit -m "Add room count target to dungeon generator"`

---

### Task 2: Batch UI (Character, Export/Import w Project, przycisk Center)

**Files:**
- Modify: `src/ui/panels/map.ts`, `src/ui/panels/project.ts`, `src/ui/panels/exportModal.ts`, `src/ui/panels/importModal.ts`, `src/ui/panels.ts`, `src/ui/mode.ts`, `src/ui/layout.ts` (tolerancja usunietych sekcji), `index.html`, `src/styles.css`
- Create: `src/ui/center.ts`

**Zachowanie:**
1. **Character mniejszy i do lewej (prosba #1):** input Character w panelu Simplified: szerokosc ~72px, wyrownany do lewej (label nad nim bez zmian), tekst w inpucie do lewej. Reszta panelu bez zmian.
2. **Export/Import w karcie Project (prosba #2):** karty `export` i `import` USUNIETE z index.html (i z listy sekcji DnD); przyciski otwierajace modale przenosza sie do karty Project: rzad `[Export...] [Import...]` (niebieski/zielony, 50/50) UMIESZCZONY nad istniejacym rzedem `[Export workspace] [Import workspace]`. exportModal.ts/importModal.ts: init nie tworzy zawartosci karty - buduje TYLKO modale i zwraca funkcje `open()` (przebudowa initu; project.ts woła je z nowych przyciskow). Modale i ich zachowanie bez zmian. Zapisane layouty z id export/import sa tolerowane (applyLayout juz pomija nieznane/brakujace - zweryfikowac). Simplified: bez zmian (karty i tak byly ukryte; przyciski w Project tez sa ukryte bo caly Project jest ukryty).
3. **Plywajacy Center (prosba #4):** `src/ui/center.ts`: staly przycisk `Center` na dole POSRODKU okna (fixed, bottom 12px, wysrodkowany poziomo), styl v1 (bialy, ramka 2px, Press Start 2P, hover jak inne przyciski), z-index ponizej modali i toastow; klik -> centerOnPaper() + markDirty (bez historii/autosave). Widoczny w OBU trybach. Toast przesuniety NAD przycisk (bottom ~56px), zeby sie nie nakladaly - jedyna zmiana toastu to pozycja.
4. Centrowanie po usunieciu kart (wezsze kolumny przy przelaczaniu trybow itd.) dziala jak dotad (withOffsetShift przy mode switch pokrywa; usuniecie kart z DOM to jednorazowa zmiana szerokosci kolumny prawej - po tej zmianie domyslne centrowanie liczy sie z aktualnych szerokosci, nic do roboty poza weryfikacja).

- [ ] **Step 1:** Character + style.
- [ ] **Step 2:** Export/Import -> Project (przebudowa initow modali, usuniecie kart, tolerancja layoutu).
- [ ] **Step 3:** center.ts + pozycja toastu.
- [ ] **Step 4:** Weryfikacja: `npm test` + `npm run build`; JEDNA sesja CDP (--use-mock-keychain): Simplified - Character maly/do lewej (screenshot); Advanced - brak kart Export/Import, przyciski w Project otwieraja modale (klik w oba, modale dzialaja, Esc zamyka), stary zapisany layout z export/import nie psuje bootu; przycisk Center na dole posrodku klika i centruje (pan w bok -> klik -> wycentrowane), toast pojawia sie NAD przyciskiem; zero bledow konsoli; kill + ps.
- [ ] **Step 5: Commit.** `git add -A && git commit -m "Move export import into project card and add center button"`

---

### Task 3: Docs + wersja 2.5.0 + pakowanie

**Files:**
- Modify: `README.md` (Project card z przyciskami Export/Import, Rooms w generatorze, przycisk Center; aktualizacja liczby kart/drzewa), `itch-page.md` (changelog "v2.5: dungeon room count, floating center button, tidier project card"), `package.json` (`"version": "2.5.0"`)

- [ ] **Step 1:** Docs (ASCII, fact-check) + wersja.
- [ ] **Step 2:** `npm run zip`; `unzip -l`; standalone file:// check (jedna sesja, --use-mock-keychain, kill + ps).
- [ ] **Step 3:** `npm test` + `npm run build` zielone.
- [ ] **Step 4: Commit.** `git add -A && git commit -m "Document v2.5 tweaks and bump version"`

---

## Self-Review (wykonany)

- **Prosby 1-4 pokryte:** T2.1, T2.2, T1, T2.3. Interpretacje przekazane userowi w rozmowie.
- **Placeholdery:** brak; generateDungeonDetailed z jawna sygnatura i wrapperem (zero duplikacji).
- **Ryzyko:** przebudowa initow modali (export/import) - behawior modali niezmienny, weryfikowany klikiem w CDP; layout po usunieciu sekcji - jawny test bootu ze starym layoutem.
